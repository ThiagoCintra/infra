package com.br.itau.login.service;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.security.authentication.UsernamePasswordAuthenticationToken;

import com.br.itau.login.domains.UserRepositoryDomain;

public class ContractServiceImpl 	implements ContractService {
	
//	private final UsernamePasswordAuthenticationToken auth;
	
//	@Autowired
//	public ContractServiceImpl( UsernamePasswordAuthenticationToken auth) {
//		this.auth = auth;
//	}
	
	@Override
	public void contract() {
//		auth.getPrincipal().
		
//		String ROLE_CONTRACT_SERVICE = auth.getAuthorities().stream()
//			.filter(a -> a.getAuthority().equals("ROLE_CONTRACT_SERVICE"))
//			.findFirst()
//			.orElseThrow(() -> new RuntimeException("User does not have access to contract service")).getAuthority();
	
	}

}
